---
- name: Uninstalling docker packages
  yum: name="{{ item }}" state=removed
  with_items:
    - docker
    - docker-common
    - container-selinux
    


#- name: Remove packages for letsencrypt compatibility
#  become: true
#  yum: name="{{ item }}" state=removed
#  with_items:
#    - "pyOpenSSL"
#    - "python-requests"
#    - "python2-cryptography"

